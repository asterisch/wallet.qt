#ifndef WALLETMODEL_H
#define WALLETMODEL_H
/*
#include <QObject>
#include <QAbstractListModel>

//#include <wallet.h>

class walletModel : public QAbstractListModel
{
    Q_OBJECT
    // vector <Wallet> wallets;
public:
    explicit walletModel(QObject *parent = 0);

    enum WalletAttr {
        NameRole = Qt::UserRole + 1,
        MoneyRole,DescriptionRole,PayMetRole,PeriodRole
    };
    walletModel();
    /*void insertWallet(QString name, QString descr, double money, QString paymethod, QDate period);
    QHash<int, QByteArray> roleNames() const;
    int rowCount(const QModelIndex &parent = QModelIndex()) const;
    QVariant data(const QModelIndex &index, int role = Qt::DisplayRole) const;
*/
signals:

public slots:
};

#endif // WALLETMODEL_H*/
